# Use the system's current stable. Pre-release we'll pin to an
# exact patch version (e.g. "1.95.0") for reproducible builds and
# auditor verification.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
